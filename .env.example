# Production backend (used for local dev and Vercel builds)
VITE_API_BASE_URL=https://restaurant.grahmind.com

# Optional: use local backend instead — comment the line above and run restaurant-backend on 5180
# VITE_API_BASE_URL=
